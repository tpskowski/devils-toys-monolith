# The original book: redistributable text lives in rules/, but the book itself
# is not ours to publish. Everything an author keeps for reference goes here.
/source/

# Locally built installer files.
/dist/
/*.devilsystem.zip

# Editor and merge leftovers
.DS_Store
Thumbs.db
desktop.ini
*.orig
*.rej
